---
node: AGENTS
layer: root
role: 入口
工坊: 文档导航
服务: 工作流
简介: 接管本项目的 agent 的顶层入口说明。
reads:
  - ENGINE/工作流总览.md
  - ENGINE/prompts/立项工坊-立项问答.md
  - ENGINE/prompts/复审修改.md
  - ENGINE/prompts/世界线宏大推演.md
  - README.md
  - ENGINE/归档结构规范.md
  - ENGINE/knowledge/知识库地图.md
  - ENGINE/templates/模板总览.md
  - ENGINE/_命名映射表-已执行.md
---

# AGENTS.md

给接管本项目的agent（Claude Code / opencode / 其他任何有文件读写与搜索能力的agent工具）看的入口说明。不管底层用什么模型（Claude / DeepSeek / 其他），下面的规则都适用。

## 这是什么项目

一个网文写作工作台：`ENGINE/`是通用写作引擎（流程规则+模板+知识库），`BOOKS/`下每个子目录是一本具体的书。**权威流程文档是 `ENGINE/工作流总览.md`，其他任何文件（包括本文件、README.md）如果和它冲突，以它为准。**

> 全项目步骤已统一为**纯中文名**（不再用 P/V/L/K/T 代号）。若在旧产物里看到 P6/V4/L2 之类代号，对照 `ENGINE/_命名映射表-已执行.md` 换算成中文名。

## 快速上手：接管项目先读机器可读地图 `ENGINE/tools/graph.json`

**接手本项目的第一步，先读 `ENGINE/tools/graph.json`。** 它是整套工作流的**机器可读地图**，一个文件就能让你在几秒内建立全局理解——有哪些步骤、分几个工坊、谁调用谁、每个 prompt 干嘛的——比通读一堆散文档快得多。怎么读：

- **`nodes[]`**：每个节点（prompt / knowledge / 模板 / 文档）。看 `简介`（一句话它是干嘛的）、`工坊`（属于哪个工坊）、`服务`（`小说`=写书管线 / `工作流`=维护工作流自身）、`度量`（被调用/产出字数/token 情况）。
- **`edges[]`**：节点间关系。`type:"pipeline"`=**管线下游**（执行先后顺序）；`type:"reads"`=**参考依赖**（这一步会加载哪些 knowledge/模板）。顺着 pipeline 边就能还原"从立项到成稿"的完整调用链。
- **`issues`**：体检（孤儿/断链/循环），正常应全空。

**读法**：先扫一遍 nodes 的 `简介`+`工坊`，对全局有个印象 → 顺 pipeline 边理清管线顺序 → 要动某一步时，再打开对应的 `ENGINE/prompts/xxx.md` 看细则。

> `graph.json` 是**给 agent 读的**；`workflow_map.html` 是同一份数据的**人类可视化**（D3 图，浏览器打开），agent 不必读 html。两者都由 `python3 ENGINE/tools/workflow_map.py` 生成。

## 最重要的一条规则：修改意见 = 自己去检索上下文，不要等人贴材料

作者对已写的正文提修改意见时（比如"改第X章第Y段，问题是XXX"），**你有文件读写和搜索能力，必须自己主动去定位并读取相关文件，不要要求作者手动粘贴**。具体怎么做，完整照着 `ENGINE/prompts/复审修改.md` 执行——里面写清楚了：

1. 怎么按目录约定定位本章正文、焦点卡、人物卡、伏笔登记表、演进状态卡、关系情感状态卡
2. 怎么建立修改的上下文理解（结构/因果/伏笔/人物/设定五个维度）
3. **改完不能就完事——要用文件搜索横向排查全书其他地方有没有因这次修改而需要跟着改的地方，找到就直接打开文件改掉，不是列个清单甩给作者**
4. 输出格式：主修改 + 连带修改清单（已执行/因内容未写出而无法执行）+ 影响说明

这条规则不需要作者每次重复贴提示词——你接到"改第X章第Y段"这类请求，直接照 复审修改 的流程走。

## 目录约定（书名目录下）

- `00-立项/`：立项书（立项工坊产出，全书总纲——新建项目/世界观与金手指/人物种子/故事种子与世界基底/故事弧大纲 动手前都先读它、产出受其约束）
- `01-设定/`：世界观设定卡、金手指卡、世界基底、人物状态维度定义、命名与术语表
- `02-大纲/`：伏笔登记表.md、演进状态卡.md、世界线纪要.md、故事弧大纲-第N弧.md、情绪蓄力卡.md
- `03-人物/`：人物卡（主角-XXX.md / 配角-XXX.md，逐人一个文件）、状态账本-XXX.md、关系情感状态卡.md
- `04-正文/`：第X章-标题.md（正文本体）
- `05-焦点卡/`：焦点卡-第X章.md、焦点卡校验报告-第X章.md、章纲节拍表-第X章.md、场面调度卡-第X章.md、状态预算-第N弧.md、章节状态清单-第X章.md

## 其他常见指令怎么处理

- 「进入立项」「开始立项，书名《XXX》」「跑立项自查」——开新书的第一步，对应 `ENGINE/prompts/立项工坊-立项问答.md` 和 `立项工坊-立项自查.md`。立项问答是**逐维度串行选择题**：一次只问一个维度、每维度现场动态生成 3-5 个贴合候选 + "自定义"，选完一个再据此问下一个；在本工具里优先用交互式选择题控件（AskUserQuestion 之类）弹给作者点选，纯聊天环境才退化成文字 A/B/C。产出 `00-立项/立项书.md`。
- 「写第N章」「跑世界线宏大推演 / 故事弧大纲 / 故事弧状态预算 / 章节状态分配」「跑章纲节拍表 / 焦点卡 / 场面调度 / 正文生成 / 润色定稿」「跑焦点卡校验 / 独立正文自查 / 最终交付检查 / 回归校验」——都对应 `ENGINE/prompts/` 下的同名文件，先读 `ENGINE/工作流总览.md` 第六节"操作话术"确认当前该跑哪一步，再打开对应prompt文件执行。
- 不确定某个步骤该输出到哪个文件——参照上面的目录约定和已有同类文件的命名方式（如`焦点卡-第17章.md`），保持一致。

## 模型能力差异提醒

不同底层模型主动使用文件搜索工具的积极性不同（有的模型不明确要求就倾向于"凭对话记忆回答"而不是"去读文件确认"）。如果发现某个模型跑 复审修改 时明显没有去搜索其他章节就给出了"连带修改清单"，要在对话里明确追加一句"你刚才有没有真的用搜索工具去查？没有的话现在去查"，强制它触发工具调用，不要接受它凭空给出的结论。

## 工作流依赖图与自检（维护工作流结构用）

**★ 硬规则(所有 agent / LLM 必须遵守,不是建议)**:只要改动了本项目任何**工作流 md**(ENGINE 下的 prompts / knowledge / templates,以及本文件、工作流总览等),**改完必须重新运行 `python3 ENGINE/tools/workflow_map.py`** 刷新依赖图(生成的 `workflow_map.html` 在**项目根目录**),并顺手维护改动文件的 frontmatter。跑完把更新后的 `workflow_map.html` 一起提交。

- 每个 md 文件顶部用 frontmatter 声明依赖与分组(机器可读):`pipeline_next`(管线下游)、`reads`(参考的 knowledge/模板)都写**相对路径**;`工坊`(所属工坊,决定地图上聚在哪个虚线框)、`服务`(取值 `小说` 或 `工作流`,决定地图上是圆还是方——服务小说=写书管线,服务工作流=维护工坊/文档)。**新增或改动 prompt / knowledge / 模板时,顺手维护它的 frontmatter(含 工坊/服务)。** 现有工坊:立项工坊 / 架构工坊 / 故事弧工坊(含世界线推演,每弧第一步·架构校验后即触发) / 章节工坊(**内容层**:定"写什么"到句意稿) / **文风工坊**(**成文层**:`正文生成`+`润色定稿` 把内容制作成有声音的正文,挂载"文风包"=可复用/可成长/可调参/带范文例库的文风单元,多套并存;★**默认融合模式**——每本书取2+套文风包为原料、一书一配方智能合成本书写作风格卡(工作流级配置见 `ENGINE/文风库/_文风模式配置.md`,机制见 `_文风库总览.md`)) / 人工反馈修复工坊(以上服务小说) · 知识库 / 模板库(服务小说的共享资源) · **维护工坊**(自优化工作流,两分支各对口:**文风优化=用词优化四步 ⟷ 优化文风工坊**、**架构优化 ⟷ 优化章节工坊内容层+全管线结构**;北极星=输入范文→拆骨架→文风差距/内容差距分别回流两分支→整个工作流自优化) / 文档导航(服务工作流)。
- 跑 `python3 ENGINE/tools/workflow_map.py` → 一键生成 `tools/graph.json`(依赖数据集,**也是上手先读的机器可读地图,见顶部「快速上手」节**)+ **项目根的 `workflow_map.html`**(D3 力导向可视化,可拖动/缩放)+ 控制台体检(孤儿 / 断链 / 循环)。**改工作流结构前先看图**,别改出断链或没人引用的死文件。
- 一次性批量补 frontmatter 的脚本:`ENGINE/tools/apply_frontmatter.py`(幂等,已有 frontmatter 的文件自动跳过)。
- 三个总揽:`AGENTS.md`(本文件,顶层入口)、`ENGINE/工作流总览.md`(写书管线权威)、`ENGINE/knowledge/知识库地图.md`(knowledge 导航);模板层索引见 `ENGINE/templates/模板总览.md`。

## 使用度量上报(优化工作流用)

**★ 硬规则**:每当你**执行完一个工作流步骤**(写正文、跑校验、改稿、跑大纲等),在收尾时调用一次度量上报脚本,把这一步的用量(字数/token/是否返工)记进度量表 `ENGINE/tools/metrics.json`:

```
python3 ENGINE/tools/update_metrics.py --node <节点名> --calls 1 [--chars N] [--tokens N] [--rework 1]
```

- **★别漏次数(2026-08-22 教训)**:每一步收尾都要报——哪怕只写一章、只跑一次校验。此前漏报到"写了数十章、`正文生成` 只记个位数",数据面板严重失真。次数最重要,字数/token 估算即可。
- **上报会自动刷数据面板**:`update_metrics.py` 写完会自动重跑 `workflow_map.py`,把新值刷进 `graph.json`+`workflow_map.html`(不用再手动跑图)。批量连报可加 `--no-sync`、最后统一刷一次。
- **完整用法只看 `ENGINE/tools/metrics_使用说明.md` 这一份说明,不要去读 `update_metrics.py` 的源码。**
- 字数/token 估算即可,趋势比精度重要;数不清就省略对应参数。历史(8-22前)少报不补估,自此如实往前积累。
- 这张表用于**优化工作流**(看清哪一步最费 token/最易返工),会显示在 `workflow_map.html`(选中节点看详情、或点"数据面板"看全表)。表的行由 `workflow_map.py` 自动维护,数值只由上报脚本累加。
